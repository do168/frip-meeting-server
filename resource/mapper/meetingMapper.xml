<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="meetingMapper">  
  <select id="getMeeting">
    SELECT
      *
    FROM
      meeting
    WHERE
      id = #{id}
  </select>

  <select id="listHostMeetings">
    SELECT
      *
    FROM
      meeting
    WHERE
      hostId = #{hostId}
  </select>

  <select id="listMeetings">
    select
      *
    FROM
      meeting  
  </select>

  <select id="createMeeting">
    insert into meeting(
      title,
      content,
      startAt,
      endAt,
      deadline,
      maxParticipant,
      place
    )
    values(
      #{title},
      #{content},
      #{startAt},
      #{endAt},
      #{deadline},
      #{maxParticipant},
      #{place}
    )
  </select>

  <select id="deleteMeeting">
    update 
      meeting
    set 
      status = 0
    where 
      id = #{id}
  </select>
</mapper>